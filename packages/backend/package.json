{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "build": "echo 'Backend: run pnpm dev to generate Convex types before type-checking'",
    "typecheck": "tsc --noEmit",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint . --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.91",
    "convex": "^1.34.1"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.41",
    "typescript": "~5.9.3",
    "vitest": "^4.1.0"
  }
}
